#pragma once

#include "Common.h"

// Simple GUI placeholder (console-based for now)
class GUI {
public:
    static void Initialize() {
        // Console-based interface is implemented in main.cpp
    }
    
    static void Shutdown() {
        // Cleanup if needed
    }
}; 